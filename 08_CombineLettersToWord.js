
// 8. A function which will take 5 arguments. All of them are 1 letter strings. It will return them as a word.

function combine(a, b, c, d, e) {

    return a + b + c + d + e
}

const word = combine('S', 'M', 'A', 'R', 'T')

console.log("Combined Word: " + word);

